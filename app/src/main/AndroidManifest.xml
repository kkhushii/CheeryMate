<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hackOn2021.cheerymate">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CheeryMate">
        <activity android:name=".ExpertChat"></activity>
        <activity android:name=".fragment.PostUploadActivity" />
        <activity android:name=".post.PostImageOnClickZoom" />
        <activity android:name=".post.PostLikeOnClick" />
        <activity android:name=".post.PostCommentActivity" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".MentorFeed" />
        <activity android:name=".FeedActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>